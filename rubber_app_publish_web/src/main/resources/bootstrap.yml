server:
  port: 38012
  servlet:
    context-path: /oa-api

spring:
  application:
    name: rubber-app-publish
  cloud:
    nacos:
      config:
        file-extension: yml
        server-addr: 192.168.1.104:28848
        namespace: rubber-dev

rubber:
  proxy:
    set:
      dbInstance: oaAdminInstance
