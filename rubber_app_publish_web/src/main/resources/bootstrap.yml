server:
  port: 38002
  servlet:
    context-path: /oa-api

spring:
  application:
    name: rubber-app-publish
  cloud:
    nacos:
      config:
        file-extension: yml

rubber:
  proxy:
    set:
      dbInstance: oaAdminInstance